{
  "name": "yws-server",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "start": "y-websocket --port $PORT --host 0.0.0.0"
  },
  "dependencies": {
    "y-websocket": "^1.5.0"
  },
  "engines": { "node": ">=18" }
}
